concern test {
    summary "CONCERN!"
    description "This is concerning."
}

//condition recursion holds when recursion is True

proposition test {
    expression "a === b"
    default value True {
        raise test 
    }
    value False
}